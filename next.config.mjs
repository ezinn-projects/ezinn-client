/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["via.placeholder.com", "a0.muscache.com", "localhost", "res.cloudinary.com"],
  },
};

export default nextConfig;
